import { describe, test } from '@jest/globals';
//import { fixture, html } from "@open-wc/testing";

describe('pwa-button component test', () => {

    /*let element: any;

    beforeEach(async () => {
        element = await fixture(html`<pwa-button></pwa-button>`);
        await element.updateComplete;
    });*/

    test('should load properly with the correct css class', async () => {
        // const buttonElement = element.shadowRoot.querySelector('button');
        // expect(buttonElement).toBeDefined();
        // expect(buttonElement.classList.contains('primary')).toBeTruthy();
    });

    xtest('when clicking the button, the event pwa-button-click should be fired', async () => {
        /*const buttonElement = element.shadowRoot.querySelector('button');
        const spy = jest.fn();
        element.addEventListener('pwa-button-click', spy);
        buttonElement.click();
        await element.updateComplete;
        expect(spy).toHaveBeenCalled();*/
    })

});
