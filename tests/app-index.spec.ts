import { describe, test } from '@jest/globals';
//import { fixture, html } from "@open-wc/testing";

describe('app-index test', () => {

    /*let element: any;

    beforeEach(async () => {
        element = await fixture(html`<app-index></app-index>`);
        await element.updateComplete;
    });*/

    test('should load the main section for routerOutlet', async () => {
        /*const routerOutlet = element.shadowRoot.querySelector('#routerOutlet');
        expect(routerOutlet).toBeDefined();*/
    });

});
