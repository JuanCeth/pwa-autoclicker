import { describe, test } from '@jest/globals';
// import { fixture, html } from "@open-wc/testing";

describe('pwa-header component test', () => {

    //let element: any;

    beforeEach(async () => {
        /*element = await fixture(html`<pwa-header title="PWA Autoclicker App"></pwa-header>`);
        await element.updateComplete;*/
    });

    test('should load properly with the correct text', async () => {
        /*const headerTextElement = element.shadowRoot.querySelector('h1');
        expect(headerTextElement).toBeDefined();
        expect(headerTextElement.textContent).toBeDefined();
        expect(headerTextElement.textContent).toBe('PWA Autoclicker App');*/
    });


});
