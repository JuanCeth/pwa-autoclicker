import { describe, expect, test } from '@jest/globals';

describe('app-game page test', () => {

    beforeEach(async () => {

    });

    test('should load ', () => {
        expect('string')
            .toBe('string');
    });

});
